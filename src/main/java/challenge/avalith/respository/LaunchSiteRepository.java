package challenge.avalith.respository;

import challenge.avalith.model.LaunchSite;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LaunchSiteRepository extends JpaRepository<LaunchSite, String> {
}
